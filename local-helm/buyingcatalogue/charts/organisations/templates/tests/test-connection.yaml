apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "organisations.fullname" . }}-test-connection"
  labels:
    {{- include "organisations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "organisations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
